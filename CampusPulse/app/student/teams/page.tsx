"use client"

import { AppShell } from "@/components/layout/AppShell"

export default function TeamsPage() {
  return (
    <AppShell title="Team Match" subtitle="Find and join teams on campus">
      <p className="mt-2 text-sm text-muted-foreground">Find and join teams on campus.</p>
    </AppShell>
  )
}
